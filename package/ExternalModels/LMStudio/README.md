# Optional Heretic and Nomic cache for LM Studio

This directory is optional. Complete Installer 2.3.2 downloads missing Heretic and embedding-only Nomic Q4_K_M files automatically from revision-pinned sources. A preload is reused only after exact filename, size, and SHA-256 verification. Heretic is the only chat LLM; Nomic is embedding-only.
