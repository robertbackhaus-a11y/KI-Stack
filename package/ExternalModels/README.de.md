# ExternalModels

Die acht ComfyUI-Modell-Dateien liegen direkt in diesem Ordner; die zwei Heretic-LM-Studio-Dateien liegen in `LMStudio`. Der Import prüft Dateiname, Bytegröße und SHA256, kopiert zunächst als `.partial` und verschiebt atomar. Ausführliche Anleitung: `docs/de/KI-Stack-Manuelle-Modellbereitstellung.md`.
