# KI-Stack OpenWebUI Agent Pack 1.8.1

Status: `TargetSystemValidated` on OpenWebUI 0.10.2.

Artifact SHA256: `e0b97be402449c20c36921979df6313a44c07f97276e4dbf78df349ec69972be`.

The unchanged profiles `KI & IT-Technik` and `Allgemein` were read back without duplicates. Integration 1.5.8 was confirmed through its managed installation marker. A real technical-profile chat and a real `features.web_search = true` chat completed against the repaired global SearXNG runtime.

The temporary API key was entered as a `SecureString`, used only in process memory, cleared after validation and is not present in source, commands, logs or reports. The key must be revoked in OpenWebUI after validation.

Agent Pack 1.8.0 remains historical but its SearXNG runtime claim is superseded by this validation.
