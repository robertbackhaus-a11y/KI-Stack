# KI-Stack Models / Workflows 1.4.1

Patch release over the target-validated 1.4.0 workflow set. Visible positive defaults in FLUX2, KREA, Pony SDXL and WAN 2.2 are neutral landscape prompts. Negative prompts, graph structure, nodes, links, models and technical parameters are unchanged; the FLUX2 API workflow is unchanged.

Seven Hugging Face model contracts are manual external dependencies because Git commit hashes are not installation sources. Pony V6 XL retains fixed Civitai model-version source `290640`. No model is embedded or redistributed.
