# KI-Stack Complete Installer 2.2.5

Updates the embedded Models / Workflows contract to 1.4.5. Manual external models have no installable URL and are never downloaded; Pony remains the fixed Civitai model-version-290640 exception with size and SHA256 verification.

The installer remains non-offline, Git-free at runtime and does not embed model binaries.
