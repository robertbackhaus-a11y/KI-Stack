# KI-Stack Complete Installer 2.2.4

Adds root `README.de.md`, `Resume-KIStack-Installer.cmd` and `Status-KIStack.cmd`, and makes `Start-KIStack-Installer.cmd` request UAC once through PowerShell 7. Agent and Image reconciliation share one in-memory `SecureString` only when either is not already compliant. Licensing notices are included; no model is embedded.
