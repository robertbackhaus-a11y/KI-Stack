# KI-Stack OpenWebUI Image Pack 1.9.2

This security patch restricts the image adapter to the configured local ComfyUI HTTP(S) origin and relative request paths. It replaces the global Bandit B310 exclusion with a narrow, tested guard and includes a deterministic SPDX-2.3 JSON SBOM asset.
