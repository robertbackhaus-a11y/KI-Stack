# External model licenses

No model weights are redistributed.

- Heretic: Apache-2.0 upstream contract; external manual provision.
- Nomic Embed Text: embedding-only external dependency; upstream license applies.
- Z-Image Turbo model, Qwen text encoder and autoencoder: external manual provision; upstream terms apply.
- WAN2.2 T2V 14B high/low models, UMT5 encoder, WAN VAE and both LightX2V LoRAs: external manual provision; upstream terms apply.

The installer verifies the declared local handoff contract and never treats an informational page as an installation payload.
