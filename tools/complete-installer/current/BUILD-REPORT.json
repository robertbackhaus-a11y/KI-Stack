{
  "schemaVersion": "2.0",
  "package": "KI-Stack Complete Installer",
  "version": "2.12.0",
  "baseVersion": "2.10.1",
  "status": "ReconcilePreserveContract_ResearchAgent_RAGProjectScope_SourceOnlyBuild",
  "previousCandidateRejected": "2.4.0-rc8",
  "previousCandidateRejectReason": "StrictMode rejected normal module results whose data object did not expose the optional reboot control status property.",
  "deterministicArchive": true,
  "sourceIntegratedBuild": true,
  "rc1Reproducible": false,
  "rc2Rejected": true,
  "rc2RejectReason": "Non-compliant components were marked Completed without invoking public payload installers.",
  "rc3Rejected": true,
  "rc3RejectReason": "Legacy OpenWebUI Image Pack ownership was rejected instead of migrated, and the transaction omitted the component error.",
  "rc4Rejected": true,
  "rc4RejectReason": "The Agent Pack branch overwrote the Complete Installer PackageRoot and made the following Visual Pack payload path invalid.",
  "rc5Rejected": true,
  "rc5RejectReason": "StrictMode rejected missing optional desktop-link hashtable keys during finalization; top-level phase evidence and direct operations rollback were also missing.",
  "rc6Rejected": true,
  "rc6RejectReason": "The code-interpreter finalizer still required the obsolete image-only tool binding and lacked automatic rollback.",
  "rc7Rejected": true,
  "rc7RejectReason": "Validation Gate 1.0.2 rejected the Complete Installer because its release validation contract was missing and safe rootless legacy nested payloads were incorrectly treated as single-root errors.",
  "rc8Rejected": true,
  "rc8RejectReason": "The Complete Installer marked Validation Gate 1.0.3 Completed without deploying its payload; installed current remained 1.0.2.",
  "rc9Rejected": true,
  "rc9RejectReason": "The plan trusted the stale components.json value 1.0.3 and skipped the real Gate upgrade although current/VERSION remained 1.0.2.",
  "rc10Rejected": true,
  "rc10RejectReason": "Target validation showed that Nomic and an unrelated LM Studio model remained selectable as chat models because the OpenWebUI connection model_ids allowlist was not deployed.",
  "rc11PreliminaryRejected": true,
  "rc11PreliminaryRejectReason": "The preliminary RC11 Agent Pack still recognized only the obsolete image-pack 1.9.0 metadata and removed both Visual Pack 2.0.5-rc1 tool bindings; the corrected RC11 uses Agent Pack 1.8.5 and component-wide rollback evidence.",
  "rc12Rejected": true,
  "rc12RejectReason": "ComfyUI 1.2.3 did not persist its version marker after content deployment, referenced an uninitialized payload test during reconciliation, and did not roll back after failed real-version readback.",
  "rc13Rejected": true,
  "rc13RejectReason": "The Agent Pack accessed Exception.Response without checking that the property existed, masking the underlying OpenWebUI API failure under StrictMode.",
  "rc14Rejected": true,
  "rc14RejectReason": "The Complete Installer deployed Agent Pack before Visual Pack although Agent Pack requires the Visual Pack 2.0.5-rc2 tool metadata during pre-mutation readback.",
  "containsModels": false,
  "containsSecrets": false,
  "containsPersonalPaths": false,
  "runtimeGitDependency": false,
  "modelPolicy": {
    "chat": "Heretic only",
    "embedding": "Nomic only",
    "visual": ["Z-Image Turbo", "WAN2.2 T2V 14B", "LightX2V high 4-step", "LightX2V low 4-step"]
  },
  "comfyUI": "1.2.4",
  "openWebUIVisualPack": "2.0.5",
  "openWebUIAgentPack": "1.9.0",
  "validationGate": "1.0.3",
  "rag": "0.4.0",
  "codexLocal": "0.1.4",
  "rcBuildValidation": {
    "script": "scripts/Invoke-KIStackCompleteInstallerRCBuildValidation.ps1",
    "status": "WindowsArtifactValidated_TargetExecutionLocked",
    "stepsPassed": "13/13",
    "deterministicDualBuildConfirmed": true,
    "codexCliArtifactValidated": true,
    "realTargetTreeUnchangedByBuild": true,
    "note": "Evidence from the 2.5.0 cycle; carried forward as still valid through 2.6.0, 2.7.0, 2.8.0, 2.9.0 and 2.10.0 since none of the installer-logic/build-pipeline code this script exercises changed in any of those cycles (2.6.0: RAG module only; 2.7.0: Codex Local module only; 2.8.0: orchestrator-level cutover/heartbeat/update UX and payload deployment hygiene; 2.9.0: ReplayComponent planning/CLI contract only; 2.10.0: a new, separate Lifecycle entry point (Update-KIStack-All) that only reads existing plan/version data and dispatches to already-existing install/upgrade contracts -- no change to New-KIStackCompleteInstallerArchive.ps1 or any other build-pipeline path this script exercises). Simulated empty target used for installer-logic validation; not a full physical Greenfield install."
  },
  "replayComponent": {
    "status": "targetValidated",
    "scope": "complete-installer package only; no other component payload changed in this cycle.",
    "change": "New -ReplayComponent <component-id> parameter on Invoke-KIStackCompleteInstaller.ps1 and Start-KIStackCompleteInstaller.ps1, threaded through New-KICompletePlan. A component listed there and already compliant is planned with plannedMode='Replay' instead of 'Skip' and re-runs its existing install/validate/backup path unchanged; only openwebui-visual-pack and openwebui-agent-pack are permitted. An unknown or non-permitted component id throws before any plan step is built, i.e. before any mutation. New-KICompletePlan's alreadyCompliant field keeps its original, honest meaning (raw compliance); a new hasReplay field is combined with it to gate Invoke-KIStackCompleteInstaller's already-compliant short-circuit so a requested replay is never silently skipped. No existing compliance check, and no Upgrade/Repair/Skip dispatch for any other component, was changed; no file is deleted and no pinned component version is changed to manufacture artificial non-compliance; replaying one component never cascades to a dependent one.",
    "validation": "Test-KIStackReplayComponent.ps1: compliant+no-replay stays Skip, compliant+replay resolves to Replay without affecting sibling components (no implicit recursion), unknown component id throws, known-but-not-permitted component id throws, a genuinely non-compliant component's normal Upgrade path is unaffected, the early-return guard is statically confirmed wired to hasReplay, and the guided OpenWebUI cutover is demonstrably entered when a step arrives with plannedMode='Replay' -- all PASS. Full existing regression suite unaffected.",
    "realTargetValidation": {
      "environment": "existing real target (C:\\KI-Stack), interactive elevated run of Start-KIStackCompleteInstaller.ps1 -ReplayComponent openwebui-visual-pack",
      "plannedMode": "Replay",
      "guidedBrowserApiKeyFlow": "PASS -- real browser login and API key generation completed interactively",
      "apiKeyStored": false,
      "visualPackReplayValidation": "PASS",
      "realBackupCreated": true,
      "imageTest": "PASS",
      "videoTest": "PASS",
      "transactionStatus": "Completed",
      "openWebUIHealthcheck": "HTTP 200",
      "comfyUIHealthcheck": "HTTP 200",
      "secondVenvOrUnexpectedMutation": false
    }
  },
  "updateKIStackAll": {
    "status": "targetValidated",
    "scope": "complete-installer package only; no other component payload changed in this cycle.",
    "change": "New Lifecycle/Update-KIStack-All.cmd + .ps1, deployed to C:\\KI-Stack, enumerates every Contracts/COMPONENTS.json component plus OpenWebUI (tracked separately) and reports InstalledVersion, PinnedVersion, AvailableVersion and a classification (UpToDate/PinnedUpdatePending/DowngradeRequired/Blocked/NotManaged) that compares only InstalledVersion against PinnedVersion and is the sole basis for automatic execution. A separate upstreamStatus (Current/UpdateAvailableUpstream/Unknown), from real read-only PyPI/GitHub lookups where a reliable upstream source exists, is purely informational and never triggers an unattended install; AvailableVersion=Unknown is the honest result for components with no such source rather than a guess. OpenWebUI executes through its own already-validated isolated adapter (Update-KIStack-OpenWebUI.cmd); every other component runs through the existing Complete-Installer-Upgrade batch, and an explicit -Component selection that would understate that batch's real scope is refused before any confirmation prompt or mutation. Also fixes Remove-KIStackKnowledgeExperiment.ps1, whose Knowledge-rollback readback validation previously only set passed:false without throwing, silently allowing a genuine validation failure to be recorded as success; it now throws on that condition, matching the sibling Code-Interpreter step's existing pattern.",
    "validation": "Test-KIStackUpdateAll.ps1 (14/14): all components already at pin -> only UpToDate; one component behind its pin -> PinnedUpdatePending; installed above the pin -> DowngradeRequired; an unmanaged requested component id -> NotManaged; upstream UpdateAvailableUpstream on an already-compliant component is reported but never auto-executed (both CheckOnly and full-run NoActionNeeded checked); sequential execution (OpenWebUI first, then the batch); a failing OpenWebUI adapter stops before the batch ever runs and its rollback detail is surfaced, not discarded; a non-EXECUTE confirmation cancels before anything runs; a partial -Component selection against a multi-component batch is blocked before mutation, naming every attached component still proceeds, and -Component openwebui alone is unaffected by an unrelated batch ambiguity; no secret handling introduced; the normal installer's ReplayComponent/Upgrade dispatch and starter registration are unaffected -- all PASS.",
    "realTargetValidation": {
      "environment": "existing real target (C:\\KI-Stack), read-only Update-KIStack-All.ps1 -CheckOnly run",
      "componentsReported": 15,
      "contractsComponentsAllUpToDate": true,
      "openWebUI": {"installedVersion": "0.11.1", "pinnedVersion": "0.11.0", "classification": "DowngradeRequired", "availableVersion": "0.11.1", "upstreamStatus": "UpdateAvailableUpstream", "upstreamSource": "PyPI:open-webui"},
      "mutationPerformed": false,
      "note": "OpenWebUI's real installed version had drifted above the pin (0.11.1 vs. pinned 0.11.0) since the 2.9.0 cycle, independently confirming DowngradeRequired classification and the same-value upstream detection both work against genuinely live, unstaged drift; the real downgrade back to 0.11.0 was deliberately not executed in this cycle -- that contract itself was already real-target validated under REG-038/2.8.0 (Update-KIStack-OpenWebUI.cmd, including a real downgrade and upgrade)."
    }
  },
  "openWebUICutoverHeartbeatManagedUpdate": {
    "status": "targetValidated",
    "scope": "complete-installer package only; no other component payload changed in this cycle.",
    "guidedOpenWebUICutover": {
      "change": "Start-KIStackCompleteInstaller.ps1 opens OpenWebUI in the default browser and guides first-login/API-key creation before the existing Visual Pack install/validate path runs; WaitingForUserAction now means only 'services reachable, key still missing', and OpenWebUI/ComfyUI unreachable beyond their readiness wait is a real error instead.",
      "validation": "Test-KIStackOpenWebUIVisualPackCutover.ps1: Skip/reachable-no-key, OpenWebUI-unreachable-after-budget, ComfyUI-unreachable, and successful token path -- all PASS.",
      "realTargetNote": "Not end-to-end re-triggered on the existing real target in this cycle: openwebui-visual-pack and openwebui-agent-pack were both already compliant (confirmed via a read-only Audit-mode plan against C:\\KI-Stack), so plannedMode resolves to Skip and the guided flow itself never executes. No existing Force/Repair/Replay contract can re-trigger an already-compliant step without deleting real files or changing a pinned component version, both of which were deliberately not done. This is a First-Install acceptance boundary, not a release defect -- the flow's own building blocks (readiness checks, SecureString handling, the underlying Visual Pack install/validate path) are independently real-target proven elsewhere in this same cycle and in prior releases."
    },
    "installerProgressHeartbeat": {
      "change": "Central Write-KICompleteStepStatus/Write-KICompleteStepHeartbeatIfDue helpers report Running/Waiting/WaitingForUserAction/Completed/Failed with a timestamp, and a heartbeat at least every ~20-30s while an existing wait loop is still active; wired into the main step loop, its failure handler, and the OpenWebUI readiness loop.",
      "validation": "Test-KIStackInstallerHeartbeat.ps1: short step produces no heartbeat, long step produces exactly one due heartbeat with elapsed runtime, WaitingForUserAction/Completed/Failed each print exactly once, orchestrator wiring confirmed via static source markers -- all PASS.",
      "realTargetNote": "Incidentally confirmed live during this cycle's real Upgrade runs against C:\\KI-Stack: heartbeat-formatted step lines (e.g. '[Codex Local] ... Running ... Completed') were observed in the actual console output of both real installer executions performed for the managed-update acceptance below."
    },
    "rc13FixtureRecoveryFix": {
      "change": "Resolve-KICompleteFailedTransactionState gained the same optional FixtureState bypass New-KICompletePlan already used, so its codex-local/integration live-compliance sub-checks can be skipped under a test fixture; no change to any real-target compliance behavior (FixtureState remains unset on every real invocation).",
      "validation": "Test-RC13FailedStateRecovery.ps1 PASS; negative control re-run without FixtureState reproduces the exact original real-path failure, confirming the real compliance path is untouched.",
      "realTargetNote": "Not applicable by nature -- pure test-harness testability fix, no real-install code path changed."
    },
    "managedOpenWebUIUpdate": {
      "change": "New Lifecycle/Update-KIStack-OpenWebUI.cmd + .ps1, deployed to C:\\KI-Stack, reads the target version from the single kernel-config.json also used by the Applications module, reuses the existing managed venv in place for both upgrade and downgrade via the same contract, performs a managed stop/restart and local healthcheck, and rolls back automatically on install or healthcheck failure.",
      "validation": "Test-KIStackOpenWebUIManagedUpdate.ps1: Skip, successful upgrade, successful downgrade, install-failure rollback, healthcheck-failure rollback -- all PASS against fixtures.",
      "realTargetValidation": {
        "environment": "existing real target (C:\\KI-Stack), via two real administrator-elevated Complete Installer Upgrade runs with a temporarily retargeted kernel-config.json, reverted to 0.11.0 afterward",
        "skip": {"outcome": "PASS", "version": "0.11.0", "mutation": false},
        "downgrade": {"outcome": "PASS", "from": "0.11.0", "to": "0.10.2", "managedVenvOnly": true},
        "upgradeBack": {"outcome": "PASS", "from": "0.10.2", "to": "0.11.0"},
        "healthcheck": "HTTP 200 after every step",
        "secondVenvCreated": false,
        "manualPipMutationOutsideScript": false,
        "finalInstalledVersion": "0.11.0"
      }
    },
    "payloadDeploymentHygiene": {
      "change": "Install-KICompleteOrchestrator now removes, per payload-type folder under installer/complete/Payload/, any destination file whose name is no longer present in the current source payload before copying the current one in -- fixing accumulated stale/misplaced payload zips that made Expand-KICompletePayload fail with 'Payload ist mehrdeutig'.",
      "validation": "Test-KIStackPayloadDeploymentHygiene.ps1: empty destination, stale-same-type versions removed (with pre-cleanup state still backed up), foreign misplaced payload removed without affecting a sibling payload type, repeated run idempotent -- all PASS.",
      "realTargetNote": "Confirmed against the real target: C:\\KI-Stack\\installer\\complete\\Payload\\CutoverRuntime contained 5 accumulated historical zips before this fix; a real Complete Installer Upgrade run after the fix reduced it to exactly the one current zip, and Expand-KICompletePayload -Name CutoverRuntime resolved unambiguously on every subsequent real run performed for the managed-update acceptance above."
    }
  },
  "codexLocalLmStudioTimingFix": {
    "status": "targetValidated",
    "rootCause": "Ensure-KILMStudioEndpointReachable's own retry budget (30 attempts * 2s = 60s) was independently timed and shorter than the managed LM Studio starter's genuine first-run contract (up to ~120s: 90s for the lms CLI to appear after LM Studio's first GUI launch, plus 30s for the server it starts to accept connections), and the starter's own process/exit code was never observed -- reproduced live during the 2.5.0 Greenfield acceptance run (Codex Local failed with 'LM Studio /v1/models ist nicht erreichbar' while the starter's first-run GUI wait was still legitimately in progress).",
    "fix": "The starter is now treated as the authoritative start/timeout contract rather than a second, independently-timed one: the default retry budget is sized to the starter's own up-to-120s window, and the starter's own process handle is tracked so a real starter failure (non-zero exit) is detected and surfaced immediately via a new starterExitCode field instead of being silently outlived by continued polling for the rest of the budget.",
    "scope": "codex-local module only; no changes to the LM Studio starter itself (KIModuleApplications.psm1) or to any other component.",
    "validation": "Test-KIStackCodexLocal.ps1: a static, zero-sleep assertion on the shipped default values (total budget >= 120s) plus fast behavioral checks that a starter's own exit code is observed immediately and that a genuine timeout still fails. Negative control confirmed: reverting the fix reproduces exactly this failure.",
    "liveTargetValidation": {
      "environment": "existing real target (C:\\KI-Stack), not a fresh Greenfield VM",
      "controlledState": "the real, already-installed LM Studio server was stopped via 'lms server stop' (GUI left running) so the managed starter was genuinely required; the lms CLI was already materialized on this machine from prior sessions, so this run exercises the starter's server-start/endpoint-wait phase, not a from-scratch first-ever GUI/CLI-materialization cold start (that sub-path remains covered by the deterministic regression test only)",
      "starterInvoked": true,
      "endpointResult": {"reachable": true, "modelCount": 3, "models": ["qwen3.6-27b-uncensored-heretic-v2-native-mtp-preserved","qwen/qwen3.6-35b-a3b","text-embedding-nomic-embed-text-v1.5"]},
      "elapsedSeconds": 4.13,
      "outcome": "PASS -- endpoint reached well within the legitimate starter window; verified independently afterward via a direct request to the endpoint.",
      "starterFailurePathProof": {
        "method": "a separate, synthetic starter (exit /b 1) under a scratch TargetRoot, not the real production starter -- side-effect-free, no real target files touched",
        "starterExitCode": 1,
        "elapsedSecondsToDetection": 5.12,
        "retryBudgetSeconds": 15,
        "outcome": "PASS -- starter failure surfaced immediately (well under the full budget) via the starterExitCode field, not silently outlived by continued polling"
      }
    },
    "greenfieldValidated": false
  },
  "ragLiveTargetValidation": {
    "execute": "passed",
    "addThenRollback": "passed",
    "replaceThenRollback": "passed",
    "removeThenRollback": "passed",
    "rollbackIdempotentOnSecondCall": true,
    "remoteFileGenuinelyDeletedNotJustDetached": true,
    "restoredContentByteVerifiedAgainstOriginal": true,
    "localStateConsistentAfterward": true,
    "embeddingRestoreStatusObserved": "NoMutation",
    "testFixtureCleanedUpAfterward": true,
    "scope": "Execute (Add/Query/Replace/Remove) and Rollback of Add, Replace, and Remove all verified against a live OpenWebUI 0.11.0 instance with before/after remote evidence, each including a repeated Rollback call confirmed as a clean idempotent no-op.",
    "regressionFinding": "A partial-failure-then-retry regression test for the Replace/Remove restore path (added 2.6.0 AP01, mirroring the pre-existing Add-path scenario) found a real defect: a retry could re-issue a remove call against remote content an earlier partial attempt had already deleted, failing the retry itself. Fixed in Remove-RAGRemoteEntry to treat an already-removed target as already-satisfied, matching the real server's own behavior; negative-control verified."
  },
  "greenfieldAcceptance": {
    "status": "PhysicalGreenfieldInstallCompleted",
    "cycle": "2.5.0 -- carried forward as still valid through 2.6.0, 2.7.0, 2.8.0, 2.9.0 and 2.10.0; no build-pipeline/payload-acquisition code changed in any of those cycles (2.6.0: RAG module only; 2.7.0: Codex Local module only; 2.8.0: orchestrator-level cutover/heartbeat/update UX and payload deployment hygiene; 2.9.0: ReplayComponent planning/CLI contract only; 2.10.0: a new Lifecycle entry point that only reads plan/version data and dispatches to already-existing install/upgrade contracts), so no new physical Greenfield run was performed.",
    "environment": "Hyper-V VM, fresh Windows install, no prior KI-Stack state",
    "transactionId": "KI-COMPLETE-20260824-152853",
    "rebootResumeAfterFirstTimeWslActivation": "passed",
    "finalExitCode": 0,
    "allRegularStepsCompleted": true,
    "visualAndAgentPackStatus": "WaitingForUserAction -- expected credential state because no OpenWebUI administrator API key was supplied by design, not an installation failure",
    "manualFileOrProductWorkaroundsOnTarget": false,
    "ragPostInstallChecks": {
      "ragModulePresent": true,
      "lmStudioEmbeddingModelAutoProvisioned": true,
      "openWebUIReachable": true,
      "sourcesJsonDefaultValid": true,
      "auditAndDryRunRunnable": true
    },
    "findings": [
      {
        "id": "receive-artifact-http-416-resume",
        "component": "models-workflows",
        "description": "Receive-Artifact issued a Range request starting at the already-complete artifact's end-of-file offset (Range: bytes=<ExpectedSize>-) whenever a fully-downloaded .partial file had not yet been moved to its final target, and the source correctly answered HTTP 416 Range Not Satisfiable. Fixed: a .partial file already at the exact expected size is now verified directly via the existing size/SHA256 contract, issuing no HTTP request; a hash mismatch fails safely through the existing invalid-artifact contract, never a silent reuse.",
        "discoveredDuring": "this physical Greenfield acceptance run",
        "fixedAndReverifiedAgainstTheSameLiveTarget": true
      }
    ]
  }
}
