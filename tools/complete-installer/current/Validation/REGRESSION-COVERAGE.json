{
  "schemaVersion": "1.0",
  "packageName": "KI-Stack-Complete-Installer",
  "packageVersion": "2.16.0",
  "coverage": [
    {
      "id": "REG-023-ROOTLESS-NESTED-ARCHIVE",
      "status": "covered",
      "method": "Validation Gate 1.0.3 extracts rootless legacy payload archives at their archive root and validates safe paths, duplicate paths, symlinks, CRC and declared SHA-256 values."
    },
    {
      "id": "REG-030-PS51-CONVERTFROM-JSON-DEPTH",
      "status": "covered",
      "method": "The RAG mock test server (launched as Windows PowerShell 5.1, whose ConvertFrom-Json has no -Depth parameter before PS6) no longer passes -Depth when reading its persisted state, avoiding a parameter-binding crash at mock startup that otherwise only ever surfaced to the client as a generic connection failure. Covered by Test-KIStackRAGStatePersistence.ps1."
    },
    {
      "id": "REG-031-PS51-SET-CONTENT-UTF8NOBOM",
      "status": "covered",
      "method": "The same PowerShell 5.1 mock process no longer uses Set-Content -Encoding utf8NoBOM (not a valid enum value before PS6, threw ParameterBindingException on every write); persistence writes use plain ascii, which is lossless for ConvertTo-Json's own pure-ASCII (\\u-escaped) output. Covered by Test-KIStackRAGStatePersistence.ps1."
    },
    {
      "id": "REG-032-START-PROCESS-ARGUMENTLIST-EMPTY-STRING-DROP",
      "status": "covered",
      "method": "Start-Process -ArgumentList silently drops a bare empty-string array element instead of passing a genuine empty argument, misbinding the following named parameter. The RAG mock launcher (Start-RAGMockOpenWebUIServer) passes the literal two-character string '\"\"' instead of an empty string for -InitialOpenAIKey, which correctly binds to an empty string in the child process. Covered by Test-KIStackRAGStatePersistence.ps1."
    },
    {
      "id": "REG-033-MODELSWORKFLOWS-RECEIVE-ARTIFACT-HTTP-416-RESUME",
      "status": "covered",
      "method": "Receive-Artifact issued a Range request starting at an already-complete artifact's end-of-file offset whenever a fully-downloaded .partial file had not yet been moved to its final target, and the source correctly answered HTTP 416 Range Not Satisfiable. A .partial file already at the expected size is now verified directly via the existing size/SHA256 contract with no HTTP request. Found and reproduced live during the 2.5.0 Greenfield acceptance run; fixed and reverified against the same live target. Covered by Tests/Test-KIStackModelDownloadContract.ps1 (models-workflows)."
    },
    {
      "id": "REG-034-CODEXLOCAL-LMSTUDIO-STARTUP-TIMING",
      "status": "covered",
      "method": "Ensure-KILMStudioEndpointReachable's own retry budget (previously 30*2s=60s) was shorter than the managed LM Studio starter's genuine first-run contract (up to ~120s: 90s waiting for the lms CLI after LM Studio's first GUI launch, plus 30s waiting for the server to accept connections), causing Codex Local to report the endpoint unreachable and fail while the starter was still legitimately starting -- reproduced live during the 2.5.0 Greenfield acceptance run. The default budget is now sized to the starter's own window, and the starter's own process/exit code is tracked so a real starter failure is detected and surfaced immediately instead of being silently outlived by continued polling. Covered by Test-KIStackCodexLocal.ps1 (a static assertion on the shipped default values, plus behavioral checks that a starter's own exit code is observed and that a genuine timeout still fails)."
    },
    {
      "id": "REG-035-GUIDED-OPENWEBUI-FIRST-LOGIN-CUTOVER",
      "status": "covered",
      "method": "Start-KIStackCompleteInstaller.ps1 opens OpenWebUI in the default browser and guides first-login/API-key creation before the existing Visual Pack install/validate path runs; WaitingForUserAction is now reported only when OpenWebUI and ComfyUI are both reachable but no API key has been entered, and either service staying unreachable beyond its bounded readiness wait is a real error instead of a silent WaitingForUserAction. Covered by Test-KIStackOpenWebUIVisualPackCutover.ps1 (reachable-no-key Skip path, OpenWebUI-unreachable-after-budget error, ComfyUI-unreachable error, successful token path). Not end-to-end re-triggered against the real target in the 2.8.0 cycle -- openwebui-visual-pack/openwebui-agent-pack were already compliant, a documented First-Install acceptance boundary, not a release defect."
    },
    {
      "id": "REG-036-INSTALLER-PROGRESS-HEARTBEAT",
      "status": "covered",
      "method": "The installer previously gave no visible sign of progress during long-running steps or wait loops. Write-KICompleteStepStatus/Write-KICompleteStepHeartbeatIfDue now report Running/Waiting/WaitingForUserAction/Completed/Failed with a timestamp and a heartbeat at least every ~20-30s while an existing wait loop is active, wired into the main step loop, its failure handler, and the OpenWebUI readiness loop, with no new sleeps or polling introduced. Covered by Test-KIStackInstallerHeartbeat.ps1 (short-step no-heartbeat, long-step single due heartbeat with elapsed runtime, one-shot terminal states, static orchestrator-wiring markers); confirmed live in real Upgrade runs against C:\\KI-Stack during the 2.8.0 managed-update acceptance."
    },
    {
      "id": "REG-037-RC13-FIXTURE-RECOVERY-COMPLIANCE-BYPASS",
      "status": "covered",
      "method": "Resolve-KICompleteFailedTransactionState unconditionally called Test-KICompleteIntegrationCompliant/Test-KICompleteCodexLocalCompliant even under a test fixture, and those checks depend on real runtime/WSL state a fixture cannot provide, causing Test-RC13FailedStateRecovery.ps1 to fail with 'Fehlgeschlagene Transaktion ist nicht recoverbar' despite identical, byte-verified version strings. The function now accepts the same optional FixtureState bypass New-KICompletePlan already used for the identical compliance-check class, skipping those two live-environment checks only when FixtureState is supplied; every real invocation (Invoke-KIStackCompleteInstaller never passes FixtureState) is unaffected, confirmed by a negative-control run reproducing the exact original real-path failure. Covered by Test-RC13FailedStateRecovery.ps1."
    },
    {
      "id": "REG-038-MANAGED-OPENWEBUI-UPDATE",
      "status": "covered",
      "method": "OpenWebUI's installed version had no dedicated, rollback-safe update path; manual pip upgrades/downgrades inside its venv were the only option. Update-KIStack-OpenWebUI.cmd/.ps1 (deployed to C:\\KI-Stack) now reads the target version from the single kernel-config.json also used by the Applications module, reuses the existing managed venv in place for both upgrade and downgrade via the same contract, performs a managed stop/restart and local healthcheck, reports Skip when already at target, and automatically rolls back to the previous version (re-verifying its version and healthcheck) on install or healthcheck failure. Covered by Test-KIStackOpenWebUIManagedUpdate.ps1 (Skip, upgrade, downgrade, install-failure rollback, healthcheck-failure rollback) and real-target validated against C:\\KI-Stack: Skip PASS, downgrade 0.11.0->0.10.2 PASS, upgrade 0.10.2->0.11.0 PASS, HTTP 200 healthcheck after every step, no second venv, no manual pip mutation outside the script, final installed version 0.11.0."
    },
    {
      "id": "REG-039-PAYLOAD-DEPLOYMENT-HYGIENE",
      "status": "covered",
      "method": "Install-KICompleteOrchestrator copied the current package into installer/complete with Copy-Item -Force, which only adds or overwrites files and never removes a destination file whose name no longer exists in the source; over repeated real deployments this accumulated stale and even foreign, misplaced payload zips per payload-type folder (5 zips found under C:\\KI-Stack\\installer\\complete\\Payload\\CutoverRuntime, one of them a wrong-type Models-Workflows archive), causing Expand-KICompletePayload to fail with 'Payload ist mehrdeutig' since it requires exactly one zip per type. Before the existing copy step, each payload-type folder under the source's Payload/ directory is now reconciled to remove any destination file not present by name in the current source folder, leaving exactly the current payload after the copy. Covered by Test-KIStackPayloadDeploymentHygiene.ps1 (empty destination, stale-version removal with pre-cleanup backup preserved, foreign-payload removal without affecting a sibling payload type, idempotent rerun); real-target validated -- a real Upgrade run against C:\\KI-Stack reduced the 5 accumulated zips to exactly one, and Expand-KICompletePayload -Name CutoverRuntime resolved unambiguously on every subsequent real run."
    },
    {
      "id": "REG-040-REPLAYCOMPONENT",
      "status": "covered",
      "method": "There was no supported way to deliberately re-run an already-compliant component's install/validate path without deleting real files or changing a pinned component version to manufacture artificial non-compliance -- the documented First-Install acceptance boundary for the guided OpenWebUI cutover (see REG-035). A new -ReplayComponent <component-id> parameter on Invoke-KIStackCompleteInstaller.ps1 and Start-KIStackCompleteInstaller.ps1, threaded through New-KICompletePlan, plans an explicitly listed and already-compliant component with plannedMode='Replay' instead of 'Skip' and re-runs its existing install/validate/backup path unchanged; only openwebui-visual-pack and openwebui-agent-pack are permitted, and an unknown or non-permitted component id throws before any plan step is built or anything is mutated. New-KICompletePlan's alreadyCompliant field keeps its original, honest raw-compliance meaning; a new hasReplay field is combined with it to gate Invoke-KIStackCompleteInstaller's already-compliant short-circuit so a requested replay is never silently skipped. No existing compliance check or Upgrade/Repair/Skip dispatch for any other component was changed, and replaying one component never cascades to a dependent one. Covered by Test-KIStackReplayComponent.ps1 (compliant+no-replay stays Skip, compliant+replay resolves to Replay without affecting sibling components, unknown/non-permitted component id each throw before mutation, a genuinely non-compliant component's normal Upgrade path is unaffected, the early-return guard is statically confirmed wired to hasReplay, the guided OpenWebUI cutover is demonstrably entered via a Replay-planned step). Real-target validated against C:\\KI-Stack: -ReplayComponent openwebui-visual-pack resolved plannedMode='Replay', the real interactive browser/API-key flow completed, apiKeyStored:false, the existing Visual Pack validation passed, a real backup was created, the real image and video tests both passed, the transaction finished Completed, and OpenWebUI/ComfyUI both answered HTTP 200 afterward with no second venv or other unexpected mutation -- closing the REG-035 First-Install acceptance boundary."
    },
    {
      "id": "REG-041-UPDATE-KI-STACK-ALL",
      "status": "covered",
      "method": "There was no single, central place to check or safely update every KI-Stack-managed component; each had its own separate, undiscoverable path (or none at all), and nothing distinguished a genuine pin-vs-installed drift from upstream project churn. Update-KIStack-All.cmd/.ps1 (deployed to C:\\KI-Stack) now enumerates every Contracts/COMPONENTS.json component plus OpenWebUI (tracked separately via kernel-config.json/the managed venv) and reports InstalledVersion, PinnedVersion and AvailableVersion per component. classification (UpToDate/PinnedUpdatePending/DowngradeRequired/Blocked/NotManaged) compares only InstalledVersion against PinnedVersion and is the sole basis for automatic execution; upstreamStatus (Current/UpdateAvailableUpstream/Unknown), derived from real, read-only upstream lookups (PyPI's own JSON API for the PyPI-distributed openwebui package; GitHub's own commits API against the Git revision already pinned in a component's own payload contract for comfyui/integration; Unknown -- never guessed -- for every KI-Stack-authored bundle with no external upstream project), is purely informational and never triggers an unattended install. -CheckOnly shows the plan without asking for confirmation or mutating anything; -Component restricts the plan; -NonInteractive skips the EXECUTE confirmation. OpenWebUI runs through its own already-real-target-validated isolated adapter (Update-KIStack-OpenWebUI.cmd, see REG-038); every other component runs through the existing Complete-Installer-Upgrade batch, which has no per-component isolation -- if an explicit -Component selection would understate that batch's real scope (naming only some of the components a real run would actually touch), execution is refused before any confirmation prompt or mutation, with a clear error naming the omitted component(s). Separately, Remove-KIStackKnowledgeExperiment.ps1's Knowledge-rollback readback validation previously only set a passed:false field in its returned object without ever throwing, so a genuine validation failure (leftover Knowledge bindings after removal) could be silently recorded as if the whole finalization step had succeeded; it now throws a clear error on that same condition, matching the existing throw-on-validation-failure pattern already used by the sibling Code-Interpreter configuration step. Covered by Test-KIStackUpdateAll.ps1 (all components already at pin -> only UpToDate; a single component behind its pin -> PinnedUpdatePending; installed numerically above the pin -> DowngradeRequired; an explicitly requested unmanaged component id -> NotManaged, not silently dropped; upstream reporting UpdateAvailableUpstream on an already-pin-compliant component is surfaced but never auto-executed; sequential execution with OpenWebUI first then the batch for any remaining component; a failing OpenWebUI adapter stops before the batch ever runs, with its rollback detail surfaced rather than discarded; a non-EXECUTE confirmation cancels before anything runs; a partial -Component selection against a multi-component batch is blocked before mutation while a selection naming every affected component proceeds; -Component openwebui alone is never affected by an unrelated batch ambiguity; no secret handling is introduced; the normal installer's ReplayComponent/Upgrade dispatch and starter registration are unaffected). Real-target validated against C:\\KI-Stack: -CheckOnly reported all 14 Contracts/COMPONENTS.json components UpToDate and OpenWebUI Installed 0.11.1 against Pinned 0.11.0 -> DowngradeRequired, with the real PyPI upstream lookup additionally reporting Available 0.11.1 / UpdateAvailableUpstream; no mutation was performed."
    }
  ]
}
